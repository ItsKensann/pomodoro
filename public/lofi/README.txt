Drop an optional image in this folder to customize the lofi.radio thumbnail:

  thumbnail.png   - any aspect ratio works; cropped to fill (object-cover) at 16:9

The app falls back to a pixel-art placeholder if the file is missing.
The YouTube iframe still plays the audio behind the scenes — the thumbnail is
just the visible surface and the play/pause control.
